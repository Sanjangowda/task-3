# task-3

>> Splitted the dataset into training and test sets in the rato of 80 and 20 using sklearn model selection's train_test_split library, chosen Sales_Volume as a target column from the dataset

>> I fitted Linear Regression model and trained it on BMW sales dataset

>> I have evaluated the model using sklearn metrics such as Mean Absolute Error(MAE), Mean Square Error(MSE) and R_square score and achieved the following results:
print("Mean Absolute Error:", mae)
print("Mean Square error:", mse)
print("R square:", r2)